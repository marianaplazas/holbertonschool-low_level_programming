#include <stdio.h>

/**
 *main - start
 *
 *
 *Return: 0 
 */

int main(void)
{
	int a;

	for (a = 48 ; a <= 57 ; a++)
	{
		putchar(a);
	}
	for (a = 97 ; onten < 103 ; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
