#include<stdio.h>
/**
 * main - entry point
 *
 * Return: always 0 if is success
 */

int main(void)
{
        char az;

	az ='123';

	while (az > 97)
	{
		putchar("az");
	}
	putchar("\n");
	return (0);
}
