#include "holberton.h"
/**
 *is_prime_number - evaluate if is a prime number
 *
 *@n: the number to evaluate
 *
 *Return: 1 if is prime if not return 0
 */
int is_prime_number(int n)
{
	int a = 2;

	if (n == 1)
		return(0)
	if (n  
}
