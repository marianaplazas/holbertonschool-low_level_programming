#include "holberton.h"
/**
 *_strcmp - compare two strings
 *
 *@s1: frist pointer
 *@s2: second pointer
 *the c was counter but Betty says a lot of characters
 *Return: dest
 */
int _strcmp(char *s1, char *s2)
{
	int c;

	for (c = 0; s1[c] != '\0' && s2[c] != '\0' && s1[c] == s2[c]; c++)
	{
	}
	return (s1[count] - s2[count]);
}
